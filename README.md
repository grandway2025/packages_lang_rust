# OpenWrt rust latest version

## How to use?

After the `./scripts/feeds install -a` operation is completed, execute the following command:

```shell
rm -rf feeds/packages/lang/rust
git clone https://github.com/grandway2025/packages_lang_rust -b 1.85.0 feeds/packages/lang/rust
```
